{
  "name": "@tsjam/logger",
  "version": "2.1.0",
  "description": "Vanilla TypeScript Logger with multiple output channels",
  "main": "lib/index.js",
  "typings": "lib/index.d.ts",
  "type": "module",
  "repository": "https://github.com/am0wa/tsjam-logger",
  "license": "MIT",
  "keywords": [
    "typescript",
    "utils",
    "logger",
    "remote logging"
  ],
  "scripts": {
    "build": "tsc -b src/tsconfig.json",
    "clean": "rimraf lib",
    "lint": "eslint src",
    "test": "jest --config jest.config.js",
    "doc:html": "typedoc src/ --exclude **/*.spec.ts --out docs",
    "doc:publish": "gh-pages -m \"[ci skip] Updates\" -d docs",
    "patch-version": "npm version patch",
    "prepublishOnly": "npm run clean && npm run build && npm run test",
    "postpublish": "git push --tags origin",
    "format": "prettier './**/**.{js,jsx,ts,tsx,css,scss}' --write",
    "prepare": "husky"
  },
  "files": [
    "lib",
    "tsconfig.json",
    "!**/tsconfig.tsbuildinfo"
  ],
  "exports": {
    ".": {
      "node": {
        "import": "./lib/index.js",
        "types": "./lib/index.d.ts"
      },
      "import": "./lib/index.js",
      "types": "./lib/index.d.ts"
    }
  },
  "devDependencies": {
    "@tsjam/eslint-config-recommended": "^0.2.3",
    "@tsjam/jest-config-recommended": "^0.4.3",
    "@tsjam/web-config-base": "^0.3.1",
    "gh-pages": "^6.3.0",
    "prettier": "^3.6.2",
    "typedoc": "^0.27.9",
    "typescript": "^5.8.3"
  },
  "prettier": "@tsjam/web-config-base/prettier.config.mjs",
  "lint-staged": {
    "*.{ts,tsx,js}": "eslint --cache --fix",
    "*.{ts,tsx,js,css,md}": "prettier --write"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
